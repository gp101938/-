<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <permission
        android:name="com.example.keyprovider.READ_KEY_DATA"
        android:protectionLevel="signature" />

    <permission
        android:name="com.example.keyprovider.WRITE_KEY_DATA"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cnm"
        tools:targetApi="31">
        <activity
            android:name=".SBModeApplication"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="com.gaopeng.test.sbshujuku.KeyProvider"
            android:authorities="com.gaopeng.test.cnm.keyprovider"
            android:exported="false"
            android:permission="com.example.PERMISSION_WRITE_KEYS"> <!-- 可选权限控制 -->
        <path-permission
            android:pathPrefix="/records"
            android:permission="com.example.PERMISSION_WRITE_RECORDS"/>
    </provider>
    </application>

</manifest>